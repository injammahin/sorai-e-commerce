@extends('layouts.store') @section('title','Order '.$order->order_number.' Confirmed | SARAI')
@section('content')<div class="wrap section"><div class="max-w-3xl mx-auto text-center"><span class="inline-grid w-16 h-16 place-items-center rounded-full bg-forest text-white text-2xl"><i class="fa-solid fa-check"></i></span><p class="eyebrow mt-7">Order {{ $order->order_number }}</p><h1 class="heading mt-2">Thank you. Your order is with us.</h1><p class="muted mt-5">A confirmation has been sent to {{ data_get($order->shipping_address,'email') }}. We will update you as the order moves.</p></div><div class="max-w-3xl mx-auto card p-8 mt-12"><div class="grid gap-4">@foreach($order->items as $item)<div class="flex justify-between border-b hairline pb-3"><span>{{ $item->product_name }} × {{ $item->quantity }}</span><strong>৳{{ number_format($item->total) }}</strong></div>@endforeach<div class="flex justify-between text-lg"><span>Total</span><strong>৳{{ number_format($order->total) }}</strong></div></div><p class="mt-6 text-sm muted">Payment: {{ ucfirst($order->payment_method) }} · Status: {{ ucfirst($order->status) }}</p></div><div class="text-center mt-8"><a class="btn btn-dark" href="{{ route('home') }}">Return to SARAI</a></div></div>@endsection
