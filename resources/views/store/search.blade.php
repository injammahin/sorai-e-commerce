@extends('layouts.store') @section('title','Search'.($term?' for “'.$term.'”':'').' | SARAI')
@section('content')<div class="wrap section"><form action="{{ route('search') }}" class="max-w-3xl mx-auto flex border-b border-black"><input class="w-full bg-transparent py-4 text-3xl outline-none" name="q" value="{{ $term }}" placeholder="Search SARAI"><button class="px-5"><i class="fa-solid fa-magnifying-glass"></i></button></form><div class="text-center my-12"><p class="eyebrow">{{ $products->total() }} results</p><h1 class="heading mt-2">{{ $term?'For “'.$term.'”':'Explore the collection' }}</h1></div><div class="product-grid">@foreach($products as $product)<x-product-card :product="$product"/>@endforeach</div><div class="pagination mt-12">{{ $products->links() }}</div></div>@endsection
