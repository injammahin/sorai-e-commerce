@extends('layouts.store') @section('title',$post->meta_title?:$post->title.' | SARAI Journal') @section('description',$post->meta_description?:$post->excerpt) @section('og_type','article') @section('og_image',asset($post->image))
@section('content')<article><header class="wrap py-16 text-center max-w-5xl"><p class="eyebrow">{{ $post->category }} · {{ $post->published_at->format('d F Y') }}</p><h1 class="display mt-4">{{ $post->title }}</h1><p class="text-lg muted mt-6 max-w-2xl mx-auto">{{ $post->excerpt }}</p></header><img class="w-full max-h-[75vh] object-cover" src="{{ asset($post->image) }}" alt="{{ $post->title }}"><div class="prose prose-lg max-w-3xl mx-auto px-5 py-16 font-serif text-xl leading-relaxed">{!! $post->content !!}</div></article>@endsection
