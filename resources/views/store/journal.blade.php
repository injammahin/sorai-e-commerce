@extends('layouts.store') @section('title','SARAI Journal — Craft, Culture & Style') @section('description','Stories from Bangladeshi artisans, handloom traditions, materials, style and culture.')
@section('content')<div class="wrap section"><div class="text-center mb-14"><p class="eyebrow">Craft · Culture · Style</p><h1 class="display mt-2">The Journal</h1></div><div class="grid md:grid-cols-2 lg:grid-cols-3 gap-10">@foreach($posts as $post)<article class="reveal"><a href="{{ route('journal.show',$post) }}"><div class="aspect-[4/3] overflow-hidden"><img class="w-full h-full object-cover duration-700 hover:scale-105" src="{{ asset($post->image) }}" alt="{{ $post->title }}"></div><p class="eyebrow mt-5">{{ $post->category }} · {{ $post->published_at->format('d F Y') }}</p><h2 class="text-4xl mt-2">{{ $post->title }}</h2><p class="muted mt-3">{{ $post->excerpt }}</p></a></article>@endforeach</div><div class="pagination mt-12">{{ $posts->links() }}</div></div>@endsection
