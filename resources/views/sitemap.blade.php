<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"><url><loc>{{ url('/') }}</loc><changefreq>daily</changefreq><priority>1.0</priority></url><url><loc>{{ route('new-arrivals') }}</loc><changefreq>daily</changefreq><priority>0.9</priority></url>@foreach($categories as $item)<url><loc>{{ $item->parent_id ? route('products.index',[$item->parent,$item]) : route('categories.show',$item) }}</loc><lastmod>{{ $item->updated_at->toAtomString() }}</lastmod><changefreq>weekly</changefreq></url>@endforeach @foreach($products as $item)<url><loc>{{ route('products.show',$item) }}</loc><lastmod>{{ $item->updated_at->toAtomString() }}</lastmod><changefreq>weekly</changefreq><priority>0.8</priority></url>@endforeach @foreach($collections as $item)<url><loc>{{ route('collections.show',$item) }}</loc><lastmod>{{ $item->updated_at->toAtomString() }}</lastmod></url>@endforeach @foreach($pages as $item)<url><loc>{{ route('pages.show',$item) }}</loc><lastmod>{{ $item->updated_at->toAtomString() }}</lastmod></url>@endforeach @foreach($posts as $item)<url><loc>{{ route('journal.show',$item) }}</loc><lastmod>{{ $item->updated_at->toAtomString() }}</lastmod></url>@endforeach</urlset>
