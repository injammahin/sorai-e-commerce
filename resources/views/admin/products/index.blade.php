@extends('admin.layouts.app') @section('title','Products') @section('page_title','Products')
@section('content')<div class="flex flex-wrap justify-between gap-3 mb-5"><form class="flex gap-2"><input class="admin-input" name="q" value="{{ request('q') }}" placeholder="Search name or SKU"><select class="admin-input" name="status"><option value="">All status</option><option value="active" @selected(request('status')==='active')>Active</option><option value="inactive" @selected(request('status')==='inactive')>Inactive</option></select><button class="admin-btn admin-btn-light">Search</button></form><a class="admin-btn" href="{{ route('admin.products.create') }}"><i class="fa-solid fa-plus"></i>Add product</a></div><div class="admin-card overflow-x-auto"><table class="admin-table"><thead><tr><th>Product</th><th>SKU</th><th>Category</th><th>Price</th><th>Stock</th><th>Status</th><th></th></tr></thead><tbody>@foreach($products as $product)<tr><td><div class="flex items-center gap-3"><img class="w-12 h-14 object-cover rounded" src="{{ asset($product->primaryImage->path) }}" alt=""><strong>{{ $product->name }}</strong></div></td><td>{{ $product->sku }}</td><td>{{ $product->category?->name }}</td><td>৳{{ number_format($product->price) }}</td><td><span class="{{ $product->stock<=$product->low_stock_threshold?'text-red-600 font-semibold':'' }}">{{ $product->stock }}</span></td><td><span class="admin-badge">{{ $product->is_active?'Active':'Hidden' }}</span></td><td><div class="flex gap-2"><a class="admin-btn admin-btn-light !p-2" href="{{ route('admin.products.edit',$product) }}"><i class="fa-solid fa-pen"></i></a><form method="post" action="{{ route('admin.products.destroy',$product) }}" onsubmit="return confirm('Move this product to trash?')">@csrf @method('DELETE')<button class="admin-btn admin-btn-danger !p-2"><i class="fa-solid fa-trash"></i></button></form></div></td></tr>@endforeach</tbody></table></div><div class="mt-5">{{ $products->links() }}</div>@endsection
