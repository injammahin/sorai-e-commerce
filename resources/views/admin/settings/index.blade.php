@extends('admin.layouts.app') @section('title','Settings') @section('page_title','Store Settings')
@section('content')<form method="post" action="{{ route('admin.settings.update') }}">@csrf @method('PUT')<div class="grid xl:grid-cols-2 gap-5">@foreach($settings as $group=>$items)<section class="admin-card p-5"><h2 class="font-semibold capitalize mb-5">{{ $group }}</h2><div class="grid gap-4">@foreach($items as $setting)<label><span class="admin-label">{{ str_replace('_',' ',$setting->key) }}</span>@if($setting->type==='textarea')<textarea class="admin-input" rows="4" name="settings[{{ $setting->key }}]">{{ $setting->value }}</textarea>@else<input class="admin-input" name="settings[{{ $setting->key }}]" value="{{ $setting->value }}">@endif</label>@endforeach</div></section>@endforeach</div><div class="sticky bottom-4 mt-5 text-right"><button class="admin-btn shadow-lg px-8"><i class="fa-solid fa-floppy-disk"></i>Save all settings</button></div></form>@endsection
