@extends('admin.layouts.app') @section('title','Dashboard') @section('page_title','Dashboard')
@section('content')<div class="grid sm:grid-cols-2 xl:grid-cols-5 gap-4">@foreach([['Orders',$stats['orders'],'fa-receipt'],['Paid revenue','৳'.number_format($stats['revenue']),'fa-sack-dollar'],['Customers',$stats['customers'],'fa-users'],['Low stock',$stats['low_stock'],'fa-triangle-exclamation'],['New messages',$stats['messages'],'fa-envelope']] as [$label,$value,$icon])<div class="admin-card admin-stat flex items-center gap-4"><span class="admin-stat-icon"><i class="fa-solid {{ $icon }}"></i></span><div><small class="text-gray-500">{{ $label }}</small><strong class="block text-xl">{{ $value }}</strong></div></div>@endforeach</div><div class="grid xl:grid-cols-[1.4fr_.6fr] gap-5 mt-6"><section class="admin-card"><div class="p-5 flex justify-between"><h2 class="font-semibold">Recent orders</h2><a class="text-xs underline" href="{{ route('admin.orders.index') }}">View all</a></div><div class="overflow-x-auto"><table class="admin-table"><thead><tr><th>Order</th><th>Customer</th><th>Status</th><th>Total</th><th>Date</th></tr></thead><tbody>@foreach($recentOrders as $order)<tr><td><a class="font-semibold" href="{{ route('admin.orders.show',$order) }}">{{ $order->order_number }}</a></td><td>{{ $order->user?->name?:data_get($order->shipping_address,'name') }}</td><td><span class="admin-badge">{{ $order->status }}</span></td><td>৳{{ number_format($order->total) }}</td><td>{{ $order->created_at->format('d M') }}</td></tr>@endforeach</tbody></table></div></section><section class="admin-card p-5"><h2 class="font-semibold mb-5">Sales by month</h2><div class="grid gap-3">@foreach($sales as $row)<div><div class="flex justify-between text-xs mb-1"><span>{{ $row->month }}</span><strong>৳{{ number_format($row->total) }}</strong></div><div class="h-2 bg-gray-100 rounded"><div class="h-2 bg-copper-500 rounded" style="width:{{ min(100,$sales->max('total')?($row->total/$sales->max('total'))*100:0) }}%"></div></div></div>@endforeach</div></section></div>@endsection
