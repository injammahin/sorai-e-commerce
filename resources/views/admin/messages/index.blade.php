@extends('admin.layouts.app') @section('title','Messages') @section('page_title','Contact Messages')
@section('content')<div class="admin-card"><table class="admin-table"><thead><tr><th>Sender</th><th>Subject</th><th>Status</th><th>Received</th></tr></thead><tbody>@foreach($messages as $message)<tr class="{{ $message->status==='new'?'font-semibold':'' }}"><td><a href="{{ route('admin.messages.show',$message) }}">{{ $message->name }}</a><small class="block text-gray-400">{{ $message->email }}</small></td><td>{{ $message->subject?:Str::limit($message->message,60) }}</td><td><span class="admin-badge">{{ $message->status }}</span></td><td>{{ $message->created_at->diffForHumans() }}</td></tr>@endforeach</tbody></table></div>{{ $messages->links() }}@endsection
