@extends('admin.layouts.app') @section('title','Orders') @section('page_title','Orders')
@section('content')<form class="flex flex-wrap gap-2 mb-5"><input class="admin-input !w-64" name="q" value="{{ request('q') }}" placeholder="Order number or email"><select class="admin-input !w-44" name="status"><option value="">All statuses</option>@foreach(['pending','confirmed','processing','shipped','delivered','cancelled','refunded'] as $status)<option @selected(request('status')===$status)>{{ $status }}</option>@endforeach</select><button class="admin-btn admin-btn-light">Filter</button></form><div class="admin-card overflow-x-auto"><table class="admin-table"><thead><tr><th>Order</th><th>Customer</th><th>Status</th><th>Payment</th><th>Method</th><th>Total</th><th>Date</th></tr></thead><tbody>@foreach($orders as $order)<tr><td><a class="font-semibold text-forest" href="{{ route('admin.orders.show',$order) }}">{{ $order->order_number }}</a></td><td>{{ $order->user?->name?:data_get($order->shipping_address,'name') }}<small class="block text-gray-400">{{ data_get($order->shipping_address,'email') }}</small></td><td><span class="admin-badge">{{ $order->status }}</span></td><td><span class="admin-badge">{{ $order->payment_status }}</span></td><td>{{ strtoupper($order->payment_method) }}</td><td>৳{{ number_format($order->total) }}</td><td>{{ $order->created_at->format('d M Y') }}</td></tr>@endforeach</tbody></table></div><div class="mt-5">{{ $orders->links() }}</div>@endsection
