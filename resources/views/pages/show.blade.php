@extends('layouts.store') @section('title',$page->meta_title?:$page->title.' | SARAI') @section('description',$page->meta_description?:strip_tags($page->content))
@section('content')@if($page->hero_image)<section class="page-hero"><img src="{{ asset($page->hero_image) }}" alt="{{ $page->title }}"><div class="wrap page-hero-content"><h1 class="display">{{ $page->title }}</h1></div></section>@else<header class="wrap pt-16 text-center"><p class="eyebrow">SARAI</p><h1 class="display mt-2">{{ $page->title }}</h1></header>@endif<section class="section"><div class="wrap max-w-4xl"><div class="prose prose-lg max-w-none font-serif text-xl leading-relaxed">{!! $page->content !!}</div>@if($page->slug==='contact')<form action="{{ route('contact.store') }}" method="post" class="card p-8 mt-12">@csrf<div class="hidden"><input name="website"></div><div class="grid md:grid-cols-2 gap-5"><label><span class="label">Name</span><input class="field-box" name="name" required></label><label><span class="label">Email</span><input class="field-box" type="email" name="email" required></label><label><span class="label">Phone</span><input class="field-box" name="phone"></label><label><span class="label">Subject</span><input class="field-box" name="subject"></label><label class="md:col-span-2"><span class="label">Message</span><textarea class="field-box" name="message" rows="6" required></textarea></label></div><button class="btn btn-dark mt-6">Send message</button></form>@endif</div></section>@endsection
